"""Pharos — a transparent, context-aware orchestration proxy and live terminal dashboard
for local LLM backends.

v0.1 "Observe" is observe-only and a pure passthrough: it never mutates a request.
"""

__version__ = "0.1.0"
